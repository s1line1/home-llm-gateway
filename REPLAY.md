# 项目推演复盘：用"从零方法论"重新推演 Edge LLM 网关

> 用途：用七步方法论重新推演本项目（feature_dev 分支实际演进），
> 检验方法是否贴合真实，并标出"如果重来会提前做什么"。
> 对照事实：DESIGN.md M1-M5 里程碑、git 历史（73365b0 初版 → 分批演进）。
> 状态：复盘文档，非实施计划。

---

## 一、需求五问（本项目实际怎么答）

| 问题 | 实际答案（演进中逐步清晰） | 方法论要求 |
|---|---|---|
| 用户是谁 | 最初"自己家"，后升维"edge 多节点"（家庭是实例） | ⚠️ 实际是**边做边升维**（家庭→edge），若重来应在 DESIGN 首行就写 edge |
| 网络环境 | LLM 机在 NAT 后、无公网 IP | ✅ 一开始就对——**agent 主动外拨**是根架构 |
| 模型在哪 | 家里本地 GPU（初期），后设想多 edge 异构 | ⚠️ 初期当"单机单模型"做，M5 才补模型路由（幸好底层存了 models，零重构） |
| 客户端是谁 | DSH/Codex/Claude Code | ✅ 一开始就定 OpenAI 兼容——对外接口慎重 |
| 规模 | 单实例、低并发（初期）；压测才探到并发边界 | ⚠️ 并发上限这类**默认值早期没查**（见五） |

**推演结论**：需求五问里 3 个答对、2 个是"后补/升维"。若重来，差别主要在**文档先行**（DESIGN 首行写 edge 定位），代码结构影响不大——因为根架构（外拨隧道）从一开始就正确。

## 二、一句话本质与最小拓扑

```
客户端 → 公网网关(认证/路由) → QUIC 隧道(agent 外拨) → 本地 LLM
```

✅ **实际正确**：git 初版 `73365b0`（QUIC 隧道 + mTLS + SSE 透传）就是这条链路。推演确认——**穿透 NAT 的"外拨长连接"是最小可行架构的骨架**，不是可选项。

## 三、选型复盘（对外慎重 / 内部轻率）

| 决策 | 实际 | 重推 | 验证 |
|---|---|---|---|
| 语言 Rust | 对 | 对 | 异步高性能，无后悔 |
| QUIC(quinn) | 对 | 对 | 多路复用 + 抗丢包，压测才见其默认值坑（见五） |
| OpenAI 兼容 API | 对 | 对 | 对外接口，DSH/Codex 直连 |
| 帧协议自研 postcard | 对 | 对 | 内部实现，可换（P3 才考虑 protobuf） |
| 配置 YAML | 后补 | 从第一天就 YAML | 实际 `10f90de` 才从 CLI flags 迁到 YAML——**重来应早期做** |
| key 存储 SQLite | 后补 | 从第一天 SQLite | 实际 `3b0665e` 才从 JSON 迁 SQLite，经历 `653decd` 误提交 keys.db 事故——**重来应早期做** |

**推演结论**：核心协议选型全部正确且无后悔；**两类"工程化地基"（配置、持久化）实际是后补的**，且后补伴随返工（keys.db 误提交事故）。方法论强调"最小闭环"——但这里暴露：**配置格式与持久化选型应在 M1 就定**，它们是"随时能换但换了要返工"的半内部决策。

## 四、分阶段演进（M1-M5）复盘

```
M1 最小链路 → M2 流式 → M3 加固 → M4 完善 → M5 模型路由
```

✅ **与方法论完全吻合**：一次加一个维度、每阶段可验证（DESIGN 每项带 ✅ 和 e2e）。推演确认这是本项目最成功的方法实践——后期加模型路由/用量计量/错误语义都是**在稳定骨架上加肉**，无推翻重构。

**但重推会调整两处**：
1. **M5（模型路由）前移为 M1 的数据设计**：不是实现前移，而是 **registry 数据结构从 M1 就存 `models`**（实际也存了，但属于"碰巧"而非刻意设计）——确保任何新增能力都是"读已有字段"而非"改注册协议"。
2. **M4.5 插入"并发与默认值体检"**：见下。

## 五、并发与默认值——本项目最大的"如果重来"

**实际教训**（压测才暴露）：
- quinn 默认 `max_concurrent_bidi_streams = 100` → 高并发压测延迟爆炸、连接被重置（`114ac8f` 修复）
- agent/gateway 两端流上限都要声明，只改一端不生效
- 两级闸门（HTTP 层 `max_concurrent_requests` + agent 层 `max_concurrency`）语义要分清

**方法论要求**：这类系统（长连接多路复用）的并发模型，应在 M1-M2 之间就做一次"**默认值体检**"：
- quinn transport 默认值：流并发上限、idle timeout、流控窗口
- rustls/axum/reqwest 默认值：连接池、超时、body 限制
- 每请求任务模型的上限设计（谁限制并发、超了返回什么）

**重推动作**：M2 末尾加一个里程碑"M2.5 并发体检"——读 quinn/rustls 文档把 transport 默认值显式写进代码注释并测试高并发（比压测撞墙早半年发现）。

## 六、安全与可观测——部分正确、部分后补

| 项 | 实际 | 重推 |
|---|---|---|
| mTLS 认证 | ✅ M1 就有（架构一部分） | 对 |
| request_id 贯穿 | ❌ 后期才补 x-request-id（`b2e3a5d`） | **M1 就埋**——后期补要改所有路径 |
| 核心指标 | ✅ M4 有 /metrics | 可接受（M1 至少有计数） |
| 日志落盘 | ❌ 后期才配 systemd 日志文件（`e558f53`） | 部署文档 M4 就该带 |

**推演结论**：方法论"安全与可观测从 M1 就埋"被验证——**mTLS 对了，request_id 错了**。重来：M1 就定义"每请求一个 ID 贯穿 HTTP/隧道/上游"。

## 七、主动划"不做"清单（实际也做了）

- ✅ 多租户/计费/Redis：TODO 明确推后（直到有第二个用户）
- ✅ 多实例/无状态化：压测证明 2C4G 余量大，单实例够
- ✅ protobuf：P3 最低优先（无跨语言需求）
- ✅ 响应缓存/请求重试：明确"不做"（LLM 请求几乎不重复）

**推演结论**：与方法论完全一致，且文档化（TODO/DESIGN 有明确"不做"区）——这是本项目防过度设计的有效实践。

---

## 总结：如果从零重来，七步会怎么走（差异清单）

| # | 步骤 | 实际（对照） | 重来差异 |
|---|---|---|---|
| 1 | 需求五问 | 家庭 → edge 升维 | DESIGN 首行直接写 edge 定位 |
| 2 | 最小拓扑 | ✅ 外拨隧道正确 | 无 |
| 3 | 选型 | 协议全对；配置/持久化后补 | **YAML + SQLite 从 M1 就定**（避免 keys.db 返工） |
| 4 | 分阶段 | ✅ M1-M5 完全吻合 | M2 末尾插"M2.5 并发体检" |
| 5 | 并发默认值 | ❌ 压测才撞 100 流上限 | M2.5 提前显式声明 transport 值 |
| 6 | 安全可观测 | mTLS 对；request_id/日志后补 | **request_id 从 M1 埋** |
| 7 | 不做清单 | ✅ 一致且文档化 | 无 |

**一句话推演结论**：本项目的方法论执行度约 80%——**根架构、分阶段、不做清单完全正确**；失分在"工程化地基（配置/持久化）与可观测（request_id/日志）后补"和"并发默认值未早期体检"。若重来，**前三周（M1-M3）不会推翻任何已有设计，只会更早做 YAML/SQLite/request_id/transport 显式化**——这本身就是对现有架构的最大背书：返工全是"地基层的提前固化"，没有一次架构级推翻。
