# 构建产物
/target/
**/target/
/dist/
/.cargo-home/
/.tmp/

# 生成的证书（含私钥，绝不提交；用 certs/gen-dev.sh 重新生成）
/certs/out/

# 运行时动态 API Key（Admin API 签发，含明文密钥）
/keys.json
/keys.json.tmp
keys.db
keys.db-wal
keys.db-shm

# 本地配置（含 API Key / admin token 明文；模板见 gateway_config.example.yml / agent_config.example.yml）
/gateway-config.yml
/agent-config.yml
/config.yml

# 系统/编辑器杂项
.DS_Store
