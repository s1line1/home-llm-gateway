# Edge LLM 网关 — 重建蓝图

> **用途**：从零重建同类项目（edge LLM 网关）的实施规格。
> 以现有 `home-llm-gateway`（HEAD `6609a8c`，216 commits）为参考实现，把"哪些必须照做、
> 哪些必须改、怎么验收"写成可执行的约束。
>
> **与 DESIGN.md 的分工**：`DESIGN.md` 回答"这个系统是什么"；本文回答
> **"重写时哪些决策不能重新论证、哪些坑必须在第一天就绕开、每条约束怎么验收"**。
>
> **依据**：`DESIGN.md` / `TODO.md` / `OPTIMIZATION.md`，加两轮全量代码审查
> （帧层含计数型分配器实测、网关层含锁与热路径审查）。
>
> **基线**：文中"现有实现"的代码位置与缺陷对应 `745e8e8`（2026-09-11，初稿钉点，
> 累计 82 commits）。此后已有 134 个提交落地，一部分批评已被修掉、一部分仍成立——
> 逐条现状见 `TODO.md`《重建蓝图 §6 未修项》。**§4.4/§4.5 的传输参数已按 `s2n-quic`
> 重写**（初稿写的是 quinn，2026-09 已迁移，参数名与默认值都不同）。
>
> **状态**：蓝图。面向新代码库，不讨论如何修改现有仓库。

---

## 0. 怎么用这份文档

重建时按顺序读三样东西，其余是支撑材料：

| 顺序 | 章节 | 产出 |
|---|---|---|
| 1 | §1 不可逆决策清单 | M0 的一页纸，先钉死不变量再写代码 |
| 2 | §6 硬性要求（12 条验收断言） | 直接当 checklist，每条都有验收方式 |
| 3 | §4.5 传输参数表 | 复制进代码注释，解释每一个值为什么是这个 |

**读法**：§3/§4/§5 是规格正文，§6 是它们的可验收投影，§7 是排期，§8 是验证体系，
§10 是"事故 → 约束"的反向索引（想知道某条要求为什么存在，查这里）。

---

## 1. 不可逆决策清单（M0 产出物）

重建的第一个产出不是 `Cargo.toml`，是**一页纸，按可逆性给决策分档**。
对照本项目的 git 历史（`73365b0` 初版起）：**没有一次架构级推翻，返工全发生在
"地基层的提前固化"上**——所以这一页纸要先把"哪些地基第一天就必须定对"标出来。

### 1.1 不可逆（架构骨架 —— 不要重新论证）

| 决策 | 理由 | 反悔成本 |
|---|---|---|
| **edge 主动外拨长连接**，网关永不主动连边缘 | 绕开 NAT / 动态 IP / DDNS；网关能精确知道边缘存活。这是唯一解，不是优化项 | 重写 |
| **每请求独占一条 QUIC 双向流** | LLM 的 SSE 是慢流，TCP 上会队头阻塞；且"一请求一流"让取消/超时/计量的作用域天然清晰 | 重写 |
| **对外 = OpenAI 兼容 API** | 客户端（DSH / Codex / Claude Code / SDK）不可控，这是唯一现实契约 | 客户端全部要改 |
| **`request_id` 贯穿 HTTP ↔ 隧道帧 ↔ 两端日志** | 排查链路问题的唯一抓手；事后补要改所有路径 | 高（但可补） |
| **帧层与传输层解耦**（只依赖 `AsyncRead/AsyncWrite`） | UDP 被封时可换 TCP+TLS，帧协议一行不改；也是 Go 移植能成立的原因 | 中 |
| **调用方凭据不跨信任域**（`authorization`/`cookie` 不到 edge/上游） | edge 被攻破时不白送一把可打公网的凭据 | 安全债 |

### 1.2 半可逆（换了要返工 —— 所以第一天就做）

这一档是"随时能换、但换了要返工"的地基层——它们的共同点不是难，而是
**晚做的代价已经发生过**：

| 决策 | 现有项目的代价 |
|---|---|
| **配置用 YAML 文件**（不是 CLI flags） | 后期才从 flags 迁到 YAML（`10f90de`） |
| **持久化用 SQLite**（不是 JSON） | 后期才迁（`3b0665e`），中间发生 `keys.db` 误提交事故（`653decd`） |
| **错误类型化**（`thiserror`，非全 `anyhow`） | 后期才做（`OPTIMIZATION.md` A3） |
| **传输层参数显式声明** | 压测才撞上 quinn 默认值（`114ac8f`） |

### 1.3 可逆（别在这上面纠结）

| 决策 | 说明 |
|---|---|
| `s2n-quic` vs `h3` | 自定义帧更简单可控；想省维护量可换 `h3`（用标准 HTTP 语义替代自定义帧），代价是少一点控制力、多一层依赖。选型与迁移的坑见 `DESIGN.md` §10 |
| `postcard` vs `protobuf` | **只有动机是"跨语言互操作/生态标准化"才值得做**。postcard 在性能和简单性上更优。若仅为协议演进，postcard 加字段本身就够（但注意 §3.6 的单向兼容陷阱） |
| `axum` vs 裸 `hyper` | 无实质差异 |
| 自研前端 vs 不提供 | 管理面板是独立工程，可后补 |

### 1.4 明确不做（防过度设计）

- **多租户 / 计费 / Redis**：直到有第二个用户
- **多实例 / 无状态化**：压测证明单实例余量大
- **响应缓存 / 请求重试**：LLM 请求几乎不重复
- **配置热加载**：无需求，复杂度高
- **微服务化**：单体优先；真要拆按 api-gateway / tunnel-manager / auth / admin 四块演进

---

## 2. 骨架：三条不变量

```
客户端（任何地方，请求带 model）
   │  HTTPS + OpenAI 兼容 API（含 SSE 流式）
   ▼
cloud-gateway（公网）   axum 入口：认证 → 限流 → 按 model 路由 → 隧道帧
   │  QUIC（UDP，mTLS，一条连接多路复用，无队头阻塞）
   ▼
edge-agent（模型所在机器）  主动拨号 + 心跳 + 断线重连
   │  HTTP（127.0.0.1）
   ▼
本地 LLM（Ollama / vLLM / llama.cpp）
```

**推论（都是设计约束，不是实现细节）**：

1. 网关**不需要**知道边缘的 IP —— 一切通过已建立的出站连接；
2. 一条连接承载 N 条流，因此**流是并发、取消、超时、计量的自然单位**；
3. 边缘断网 → 心跳超时 → 自动摘除 → 客户端得到 503 而非悬挂；
4. 边缘重连 → 同 `agent_id` 接管旧连接（但要配 §6-R12 的唯一性约束，否则见 §10 事故 3）。

---

## 3. 帧与数据层规格

### 3.1 帧格式（新设计）

现有实现是 `[u32 大端长度][postcard 序列化枚举]`，类型 tag 藏在 postcard 载荷第 0 字节，
且**枚举变体序号就是线格式**（`Register=0x00` … `Error=0x07`）。
后果：任何人插入/删除/重排一个变体，就是**静默的破坏性线格式变更**，
类型系统和现有测试都抓不到（roundtrip 测试两端同源，永远自洽）。

**新设计用固定头**：

```
帧 = [ u32 BE 长度 ][ u8 协议版本 ][ u8 帧类型 ][ u64 BE request_id ][ 载荷 ]
     └─ 4B ────────┘ └─ 1B ──────┘ └─ 1B ─────┘ └─ 8B ────────────┘
     len 计的是"版本+类型+request_id+载荷"的字节数，不含自身
```

| 字段 | 作用 |
|---|---|
| `len` | 分帧；**读前必须先校验**再分配（§6-R2） |
| `协议版本` | 显式协商；不匹配时**明确拒绝并说明**，而不是炸在反序列化层 |
| `帧类型` | 固定偏移 → 可快速分派、可做兼容性检查、增删变体不再是破坏性变更 |
| `request_id` | 提到固定偏移 → 可校验"流即会话"（§3.2）、可用于两端日志对账 |

载荷仍可用 `postcard`（或任何紧凑二进制编码）—— **把 tag 提出来就够，
不必为此换 protobuf**。控制帧（`Register`/`Heartbeat`/`Cancel`/`Error`）可省略 `request_id`
或用 0 占位，但**宽度必须固定**，否则回到"靠位置对齐"的老问题。

> **契约测试**：为每种帧写 **golden bytes**（字面量字节序列）断言，而不是只做
> "自己编码自己解码"的 roundtrip。后者对任何自洽的编码方案都会通过，
> 无法发现"变体序号漂移"这类事故。

### 3.2 分帧与状态机

**读侧唯一实现**是渐进式缓冲读取器（现有项目的 `FrameReader`，见 `proto/io.rs:57-136`）：

```rust
pub struct FrameReader<R> {
    inner: R,
    buf: Vec<u8>,        // 已收到、尚未组成完整帧的字节
    want: Option<usize>, // 当前帧总长；None = 头部尚未凑齐
}
// next(): 先 take_frame()（纯同步、不 await）→ 不足则 read() 追加
// take_frame(): 凑齐头部 → 校验长度 → 不足返回 None → 否则切出载荷并解码
```

**硬性要求**：

1. **半读状态必须在结构体字段上**，不能是 future 的局部变量 —— 这是取消安全的全部要点（§3.3）；
2. **唯一的 await 必须是可取消安全的 `AsyncReadExt::read`**（tokio 保证落败 = 未读到任何数据）；
3. 解析逻辑（`take_frame`）**全程不 await**；
4. **不对外暴露基于 `read_exact` 的读帧函数**。现有项目保留了 `read_frame`（`proto/io.rs:32-55`）
   并在注释里写"不可安全取消"，但仍在网关三处配合 `timeout` 使用（`http_proxy.rs:194/262/416`）——
   那是**脆弱的巧合**：超时后立刻放弃整条请求才没出事，谁加一次重试就复现原 bug。新代码应让
   这类 API 不存在，而不是靠注释警告。
5. **流即会话可断言**：每条请求流的首帧必须是 `ProxyRequest`，其后所有帧的 `request_id`
   必须等于该流的值。不符即错（可日志、可告警、可测）。现有项目把 `request_id` 在读响应时
   全用 `..` 丢弃（`http_proxy.rs:418/431/444`），这个不变量完全没有护栏。

### 3.3 取消安全（规格 + 测试范式）

**为什么这是最高优先级**：在 `select!` 里用不可取消安全的读，落败时丢掉半读字节
→ 由于分帧是"长度前缀+载荷"，丢字节即**错位** → 下一次读把载荷当长度前缀解析
→ **`Cancel` 帧被静默丢弃** → 取消传播失效 → **上游 token 继续白烧**。

表象极隐蔽：两端进程都健康、日志正常、只有账单和延迟在变。
现有项目的 commit `d3f5360` 就是这个 bug。

**可推广的规律**：任何要在 `select!` 里用的 async 操作，先问一句
**"落败时状态在哪？"** —— 状态在局部变量 = 危险；在 `self` 上 = 安全。

**测试范式（直接搬，这是本项目最值钱的测试资产）**：
造一个"每次只吐 2 字节、偶尔返回 `Pending`"的读取器（`DribbleReader`），
保证 future 一定被 drop 在**帧中间**而不是帧边界，用 `select! { biased; ... }`
强制读分支落败，然后断言两帧仍能完整读出。见 `proto/io.rs:276-355`。

> 不要试图靠 code review 判断"这段有没有取消安全问题"——用这个测试。

### 3.4 EOF 与错误语义分级

必须区分四种情况（现有项目前两种正确、第三种有 bug）：

| 情况 | 语义 |
|---|---|
| EOF 恰好落在**帧边界** | 正常关闭 |
| EOF 落在**载荷中途** | 错误（链路被截断） |
| EOF 落在**头部（前缀）中途** | **错误** |
| 长度前缀超过上限 | 错误，**且不分配** |

> ⚠️ **现有实现的反面案例**：`read_frame` 只要 `read_exact(&mut len_buf)` 报
> `UnexpectedEof` 就返回 `Ok(None)`（`proto/io.rs:37-41`），**不管已读到 1、2 还是 3 个字节**。
> 于是越界截断被当成正常关闭，上层误判为"上游提前收流"，排障时看到误导性的 502 而不是
> "帧被截断"。而 `io.rs:357-359` 的测试注释声称它和 `FrameReader`"语义完全相同"——
> **该差异没有任何测试覆盖**。新代码必须为这四格各写一条断言。

### 3.5 缓冲与上限

| 项 | 现有值 | 重建要求 |
|---|---|---|
| 单帧上限 | `MAX_FRAME = 64 MiB`（硬编码，不可协商） | **降到 4–8 MiB**；**按帧类型分设**（控制帧应远小于 body 帧）；**帧头可协商**——否则一端的收紧就是另一端的事故 |
| 读侧预分配 | `read_frame` 凭 4 字节前缀即 `vec![0u8; len]` | **渐进增长**，按实际到达字节扩容 |
| 读缓冲容量 | `buf.drain(..total)` 不收缩 → 出现过一个 64 MiB 帧后该流 64 MiB 容量常驻到请求结束 | 加容量收缩策略 |
| 响应 chunk 通道 | `mpsc(32)` —— **条数有界、字节无界**（单条可达 64 MiB → 最坏 2 GiB） | **按字节**设界（信号量或单 chunk 上限） |
| 请求体 | 全缓冲，16 MiB 硬编码上限 | 上限可配；明确"请求方向不流式"是**有意的**取舍（要解析 `model` 才能路由） |

**实测数据（供建立量级直觉，release / Apple Silicon）**：

| 操作 | 64 KiB body 帧 |
|---|---|
| `FrameReader` 路径分配 | **7 次 / 262 KB**（≈4× wire） |
| `read_frame` 路径分配 | 2 次 / 131 KB |
| 写+序列化 | 42.7 µs ≈ **1.43 GiB/s** |
| 读+反序列化 | 43.2 µs ≈ **1.41 GiB/s** |
| 敌意前缀（声明 64 MiB、0 字节载荷） | `read_frame` 立刻申请 **67,108,921 B**；`FrameReader` 只申请 **65 B** |

> 结论：**瓶颈在分配/拷贝，不在编解码**。（现有 `TODO.md:185` 称"~2 GiB/s 已是 memcpy 级"，
> 实测低约 30%。）

### 3.6 零拷贝：先把数据类型定下来

**性能可以后补，数据类型的契约不行** —— 改帧字段类型要动协议。

- 帧的 body/chunk 字段**从第一天就用 `Bytes`**，不用 `Vec<u8>`；
- 解出载荷后用 `buf.split_to(n)` 切分，而不是 `to_vec()` 再让反序列化分配一次；
- 现有项目 `Bytes::from(chunk.clone())`（`http_proxy.rs:419`）是**纯浪费**——
  `chunk` 已是解构出的所有权值，直接 `Bytes::from(chunk)` 即可
  （注意别破坏后面 `usage.observe(&chunk)` 的顺序）；
- agent 侧 `bytes.to_vec()`（从 reqwest 的 `Bytes` 拷进 `Vec`）同样可免。

### 3.7 协议演进与版本

- **`版本` 字段从第一版就要有，而且必须被读取和校验**。
  ⚠️ 现有实现的反面案例：agent 认真填了 `Register.version`，网关用 `..` 把它**丢弃**
  （`quic.rs:64-69`），`Registry::register` 签名里也没这个参数 —— **协议版本只写不读**。
  ALPN 固定 `b"h3"` 只保证"都是本协议"，不区分版本。
- **兼容性只单向成立**（实测）：载荷**多**若干字节（新发→旧收）→ 成功且静默忽略；
  载荷**少**若干字节（旧发→新收）→ `DeserializeUnexpectedEnd` **硬失败**。
  所以"加字段天然向后兼容"是**错的**；版本校验是必需项，不是锦上添花。
- 迁移策略：**一次性切换 + 版本校验**（拒绝不匹配的对端并给出明确错误），**不做双协议共存**。

---

## 4. 流量与并发规格

### 4.1 闸门分层（四个，拒绝的位置和语义都不同）

现有项目的设计是对的，照搬：

| 闸门 | 作用 | 状态存放 | 超限响应 |
|---|---|---|---|
| per-API-key 令牌桶 | 防单 key 滥用 | 进程内 | 429 |
| HTTP 全局在途 | 防多 key 总和压垮单实例 | `AtomicU64` | 429 |
| per-agent 在途 | 尊重边缘真实容量（由 agent 声明 `max_concurrency`） | 每个注册条目的 `Arc<AtomicU32>` | 429 |
| （可选）排队 | 现有设计"满即 429，不排队" | — | — |

**要求**：三种 429 必须**分源计数**（§6-R11）。现有项目三者全进
`hlmg_requests_total{status="429"}` 一个桶，而 TOCTOU 类事故的第一症状恰恰是 429 突增。

### 4.2 计数一律 RAII 释放（最重要的一条）

**为什么**：客户端中断时，`hyper` 会**直接 drop 在途的请求 future**，
尾部代码永不执行 —— 任何"函数跑完再释放"的方案都会永久泄漏计数。

现有项目的注释（`metrics.rs:183-190`）值得原文抄进新代码：

> 释放必须挂在 Drop 上，不能依赖调用方"await 之后"的尾部语句：客户端中断时 hyper 会
> 直接 drop 在途的请求 future，尾部代码永不执行——槽位会永久泄漏，配了
> `max_concurrent_requests` 的网关会被**一次**中断打成此后全部 429（只能重启）。

**规则**：

1. 计数**必须**在 `Drop` 里减；
2. 守卫**不可** `Clone`/`Copy`（会重复释放）；
3. 守卫的生命周期要绑到**正确的范围**——见下条；
4. 心智模型与 Go 的 `defer` 不同：**Drop 只在 future 被丢弃时触发，不能假设"函数跑完了"**。

**生命周期绑定（易错点）**：SSE 长流的 body 在 handler 返回后**还在流**。
所以闸门票据必须绑到**响应 body 的生命周期**，而不是 handler 的返回。
现有项目用 `BodyExt::map_frame` 把票据挂进 body（`http.rs:312-317`），做法正确。
不这么做，闸门只覆盖首字节，**恰好在最重要的负载上失效**。

### 4.3 原子占位必须用 CAS

```rust
// 正确：用 fetch_update 的条件 CAS，零锁
entry.inflight.fetch_update(Ordering::Relaxed, Ordering::Relaxed, |n| {
    (n < entry.max_concurrency).then_some(n + 1)
}).is_ok()

// 错误：if n < max { n += 1 } —— 并发下超卖
```

HTTP 全局闸门同理，但用 `fetch_add` 的**旧值**判定（不是"先加再读"）：

```rust
let prev = active.fetch_add(1, Relaxed);
if limit > 0 && prev >= limit as u64 { active.fetch_sub(1, Relaxed); None } else { Some(Admission{..}) }
```

> 现有项目这两个写法都对（commit `b000b3b` 记录了修复前的事故原文：
> *"two concurrent requests each read active=2 after both had incremented but before either
> had checked, so both were wrongly rejected - a classic check-then-act race invisible to
> serial unit tests."*）
>
> **但 HTTP 侧仍有良性残留**：`fetch_add` 与超限时的 `fetch_sub` 之间存在窗口，期间
> `active` 高于 limit，并发 `try_enter` 可能看到虚高的旧值而**误拒**（方向安全，不会超发）。
> 重建时可考虑用 CAS 循环消除。

**关键设计**：计数器**放在每个注册条目内**（`Arc<AtomicU32>`），不是放在锁里的字段。
这样守卫能在锁外存活，锁只在选 agent 的瞬间持有。

### 4.4 背压必须端到端有界

现有项目最漂亮的一处（链路已逐跳验证成立）：

```
客户端 TCP 慢
  → hyper 不 poll 响应 body
  → 通道不再被消费
  → mpsc(cap N) 填满
  → forward_body 的 tx.send().await 挂起
  → 不再 read_frame(recv)
  → s2n-quic 单流接收窗耗尽
  → agent 的 write_frame 挂起
  → agent 不再 poll 上游 body stream
  → reqwest 停止读 TCP → 上游 LLM 写阻塞
```

**这条链的存在完全依赖于每一跳都有界。** 任何一跳无界（`unbounded_channel`、
按条数而非字节、连接级接收窗无上界）→ 慢客户端 + 快上游 = 内存打满。

参考上界（`s2n-quic` 默认；现有仓库**只设了流额度，没设数据窗口**）：

| 参数（s2n-quic 名） | 默认值 | 作用域 |
|---|---|---|
| `with_data_window` | `InitialMaxData::RECOMMENDED` = 3 750 000 B | **连接级**接收信用 |
| 三个 `*_data_window`（bidi-local / bidi-remote / uni） | 同上，均 = 3 750 000 B | **每流**接收窗 —— 这才是写侧挂起的真正阈值 |

> 与 quinn 的关键差别：s2n-quic 的默认值由 `compute_data_window(150 Mbps, 100 ms RTT, 2)`
> 算出，是个**有限值** 3.75 MB；而 quinn 的连接级 `receive_window` 默认是 `VarInt::MAX`
> （无界）。所以"默认等于无约束"这句对 quinn 成立、**对现在的传输栈不成立**。

### 4.5 传输参数表（复制进代码注释）

传输栈是 `s2n-quic`（`Cargo.toml: s2n-quic = "1.88"`）。**参数名与 quinn 完全不同，别把
quinn 的名字抄进来**：s2n-quic 全在 `Limits` 上用 `with_*` setter，窗口类一律叫
`*_data_window`，且校验上限 `u32::MAX`（4 GB）。

| 参数（s2n-quic 名） | 建议起点 | 现有实现 | 为什么 |
|---|---|---|---|
| `with_max_open_local_bidirectional_streams` | ≥ 并发目标 | 已设，默认 1024（`max_open_tunnel_streams`） | **默认只有 100**（`InitialMaxStreamsBidi::RECOMMENDED`），高并发会撞墙；**排满时不报错而是排队**（见注 ①） |
| `with_max_open_remote_bidirectional_streams` | 同上 | agent 已设 1000 | 可用额度 = `min(本地额度, 对端额度)`，**两端都要够** |
| `with_data_window` | 目标吞吐 × RTT | **两端都没设**，吃默认 3.75 MB | 连接级信用。默认值对 0.4 MB/s 的出口是过度宽松，但**有界** |
| 三个 `*_data_window` | **与流上限成对设置** | 都没设 | 每流接收窗，`N × 窗口` 才是最坏接收缓冲（见注 ②） |
| `with_max_idle_timeout` | 20 s 量级 | agent 已设 20 s | 不设时默认 `MaxIdleTimeout::RECOMMENDED` = 30 s；挂死兜底 |
| `with_max_handshake_duration` | 30 s | agent 已设 30 s | 默认 `MAX_HANDSHAKE_DURATION_DEFAULT` = 10 s，高并发下握手超时（见注 ③） |
| keep-alive（`keep_alive(true)`） | 开启 | agent 已开 | 周期 = `min(协商空闲超时 × 3/4, max_keep_alive_period)`；不保活会被对端按空闲超时关掉 |

**注 ①（流额度排满是排队，不是报错）**：`open_bidirectional_stream()` 在额度用尽时
**不返回错误，而是等待额度回收**。上游一慢，第 `额度+1` 条请求就排过 `tunnel_op_secs`，
被误判成"隧道已死" → 摘除健康 agent → 重连期间注册表为空 → **全量 503**。
所以 `流额度 ≥ 并发目标`，且摘除判据必须区分"忙"与"死"。

**注 ②（窗口与流上限成对）**：`流上限 × 每流窗口` 才是最坏接收缓冲——**流上限调高时
必须同步收紧窗口**。"ceiling 不是预分配、空闲不耗成本"这句话**只在空闲时成立**。

**注 ③（握手超时会连锁）**：高并发时新连接握不上手 → 心跳超时 → 断开重连 → 握手又超时，
形成重连风暴；真正的退避要交给重连的指数退避，而不是靠缩短握手超时。

> ⚠️ **两端都要声明，只改一端不生效**（QUIC 传输参数是双方协商的）。
> 这是本项目最贵的一次教训——它只在高并发压测里才暴露，所以重构时
> 做成独立里程碑 M2.5，而不是等压测撞墙。

### 4.6 超时矩阵

**核心原则：超时是"逐帧空闲"而非"总时长"** —— LLM 响应可以合法地慢几分钟，
总超时会杀掉正常长流；空闲超时既能兜住挂死又兼容长流。

| 超时 | 参考值 | 缺失后果 |
|---|---|---|
| 转发空闲（逐帧） | 120 s | — |
| **绝对时长上限**（宽松，如 30 min） | **必须补，但要宽** | 现有项目没有：持续吐字节的流可以**无限期**占住并发槽。⚠️ **不要设成紧的总超时**——LLM 长流是合法的，用紧超时杀长流是错的（现有 `DESIGN.md` 正是以此为由不做总超时）。正确做法是二者分开：**"流能活多久"给一个宽松的绝对上限**，**"槽位何时归还"必须与流的存活解耦**（见 §4.7：客户端已断开时槽位应立即归还，而不是等流自然结束） |
| agent 失联判定 | 15 s（配 5 s 心跳） | 慢摘除 |
| QUIC 空闲 | 20 s | — |
| **TLS 握手** | **必须补** | 连上不发 `ClientHello` 可永久占一个任务 + socket（慢速攻击面） |
| **`open_bi()`** | **必须补** | 流额度耗尽时可长时间挂起 |
| **隧道写帧** | **必须补** | 对端发送窗满且不读 → 挂起 |
| **控制流帧读取** | **必须补** | 见 §4.7 |
| HTTP 请求头读取 | 按需 | 慢速 header 攻击 |

### 4.7 取消传播与"卡住"的边界

**取消来源与路径**：

| 场景 | 要求 |
|---|---|
| 客户端在响应头前中断 | 守卫随 future drop 归还；向 agent 发取消信号（现有项目靠 `SendStream::drop` → FIN 隐式传递，agent 侧把 `None` 当取消，可行但隐式） |
| 客户端流式中途断开 | **必须即时**发 `Cancel` 帧并释放槽位 —— 见下方反例 |
| 空闲超时 | 发 `Cancel` + 归还 |
| 隧道死亡 | 对客户端给出明确错误码 + 摘除 agent |
| 进程 SIGTERM | 先停 accept → 排空窗口 → 再退（§6-R12） |

> ⚠️ **现有实现的反面案例（真实并发正确性缺口）**：
> `forward_body` **只在 `tx.send()` 失败时**才发现客户端断开（`http_proxy.rs:419`）。
> 若客户端断开后上游恰好**不再产出任何 chunk**（LLM 正在"思考"、首 token 之前的静默期），
> 任务阻塞在 `read_frame(recv)` 上，`tx.send` 永不被调用 →
> **该请求的 agent 槽位要等到 `idle_timeout`（默认 120 s）超时才释放，`Cancel` 帧也不会发出**。
>
> 而这恰是**最常见的交互形态**：用户看到卡顿就取消。后果是"上游明明空闲、
> 新请求却 429 `agent at capacity`"，且 HTTP 层闸门不受影响 —— 现象只出现在 per-edge 容量上，
> 极难归因。
>
> **修法**：把 `tx.closed()` 与 `read_frame` 放进同一个 `select!`，取消即时传播、槽位即时释放。

**控制流队头阻塞**（现有项目同样存在）：控制流接受循环是**串行**的且读帧**无超时**
（`quic.rs:57-63`）。一个卡住的 agent 只要开一条 bidi 流不发帧，后续
`Register`/`Heartbeat` 全部无法处理 → `last_seen` 过期 → 该 edge **静默停止接流量**，
而连接、进程、管理接口看起来都正常。**要求**：控制流读取加超时，并允许并发处理。

### 4.8 锁纪律

**硬性要求：任何锁不得跨越 `.await`。**

现有项目这一点做得好（`OPTIMIZATION.md` C1 把 `Mutex` 换成 `RwLock` 后，
所有持锁函数都是同步 `fn`，guard 在函数返回前析构）—— 值得肯定，
它把最难查的死锁/长尾问题在结构上排除了。

**但要注意"锁范围包含慢操作"这一类**（不是跨 await，但仍会串行化）：

| 反例 | 后果 | 修法 |
|---|---|---|
| argon2 校验（10–30 ms CPU）期间持有 key 表**读锁** | 读锁不互斥读锁，但 `create`/`delete` 需要**写锁** → 高并发下写者被饿死，运行时增删 key 卡住 | 先取出 `(hash, enabled)` 快照 → 释放锁 → 再校验 |
| 每请求一次 SQLite UPSERT，共用单一 `Mutex<Connection>` | 全局串行点；rusqlite 默认 5 s busy timeout | 合并批量写 / 单写线程 + 队列 |

---

## 5. 可观测性规格

### 5.1 指标：每个都必须带正确的标签

| 指标 | 要求 |
|---|---|
| 请求计数 | **按状态码 + 拒绝来源**分桶（见 §4.1） |
| 在途数 | HTTP 层 + **每个 agent 的在途数**（后者是"edge 配额被占满"的唯一可告警信号） |
| 延迟 | **必须有直方图/分位数**。"p99 变差"不能从求和值看出来 |
| 字节数 | 出入两个方向都要 |
| agent 数 | **健康数**，不是注册表条目数 —— 见下 |
| 隧道入口存活 | ✅ 现有项目做得对：为"静默死亡"专门造一个 Drop 守卫型 gauge（`hlmg_quic_accepting`）。`accept()` 返回 `None` 时进程、HTTP、systemd 全都显示健康、日志一行都没有，**只有 gauge 能告警** |
| 重连计数 | counter —— 但**必须同时看 rate**，见 §10 事故 3 |

> ⚠️ **现有实现的语义错误**：`hlmg_agents` 的 HELP 文本写 "Registered healthy agents"，
> 但传入的是 `registry.len()`（HashMap 条目数，只在连接关闭时摘除）。
> 心跳停止但连接仍活着的 agent **仍被计入**，而它已经被健康过滤拒绝接流量 →
> 指标比实际可用 agent 数**偏高**，且与 `/v1/models` 的聚合结果**互相矛盾**。

**必需标签**：`agent_id`、`model`、拒绝来源。
现有项目 `hlmg_requests_total` 唯一的 label 是 `status`，`TODO.md` 自己记下了
"将来加按 agent 的容量/负载指标时会咬人"。

### 5.2 日志：每行必须能回答三个问题

**要求**：请求路径上的日志必须携带 `request_id` + **`agent_id`** + `model` + 状态码 + 耗时。

> ✅ **这一条现有实现已经补上**（`745e8e8` 时它确实是最大的诊断缺口）：注册条目的结构体现在
> 带 `agent_id` 字段，请求路径上的日志也带上了它。重建时**照搬，别退回去**——
> "网关答不出这次请求去了哪台 edge"是最贵的排查成本。

**日志分级**：正常流量按 DEBUG 门控（避免刷屏），4xx/5xx 提升到 INFO/ERROR。
配一个开关让压测时能关掉逐请求日志。

### 5.3 健康检查

- **必须豁免准入闸门**。⚠️ 现有实现中 `/healthz` 走完整准入 ——
  闸门打满时健康检查返 429 → LB 摘除 / systemd 重启循环 → **把"慢"放大成"全挂"**。
- **必须有深度**：至少检查隧道入口存活、注册 agent 数、持久化可写。
  现有实现恒返 `"ok"`。

---

## 6. 硬性要求：12 条验收断言

> 这 12 条是 §3/§4/§5 的可验收投影。每条都有"反面案例"（现有代码位置）
> 和"验收方式"，可直接当 checklist 用。
>
> ⚠️ **"反面案例"列钉在 `745e8e8`，是当时的状态、不是现状。** 此后已补齐的：
> R7 的票据绑响应 body（钉点时即做对）、R10 的 `open_bi`/写帧/agent 握手三项超时、
> R11 的 `agent_id` 进日志与按 `reason` 分源的 agent 拒绝计数、§5.1 的 `hlmg_agents`
> 语义（已拆出 `hlmg_agents_healthy`）。**仍未修的项已搬到 `TODO.md`**，别在这里维护第二份。

| # | 要求 | 反面案例（`745e8e8`） | 验收方式 |
|---|---|---|---|
| **R1** | 帧头固定且自描述：`[len][version][kind][request_id][payload]`，`kind` 用**显式 discriminant** | 类型 tag 藏在 postcard 载荷里，变体序号即线格式（`frame.rs:6-43`） | 每种帧的 **golden bytes** 断言；加一个"打乱变体声明顺序"仍能通过编码测试 |
| **R2** | 读前校验长度再分配；上限按帧类型分设、可协商 | `MAX_FRAME=64 MiB` 硬编码；声明 64 MiB 前缀即申请 67 MB（`io.rs:11,50`） | 计数型分配器断言：敌意前缀下分配量为 O(1) 字节 |
| **R3** | 唯一读路径可取消安全；不存在 `read_exact` 包装的公开读帧 API | `read_frame` 不可取消却仍在 `timeout` 中使用（`io.rs:32`, `http_proxy.rs:194/262/416`） | `DribbleReader` + `select!{biased}` 范式（`io.rs:331-355`）；API 层面审查 |
| **R4** | EOF 四格分明：帧边界 / 载荷中途 / **头部中途** / 超上限 | 头部 1–3 字节截断被误判为正常关闭（`io.rs:37-41`），且注释谎称与 `FrameReader` 一致 | 四格各一条断言 |
| **R5** | 解码/方向/`request_id` 错误 → **只 reset 该流**，不摘连接 | 控制面 `?` 直接退出循环 → 整台 edge 被摘除（`quic.rs:63` + `46-48`），且被 e2e 固化为期望 | e2e：发坏帧后该 agent 仍在册、其他请求正常 |
| **R6** | "流即会话"可断言：首帧必须 `ProxyRequest`，后续帧 `request_id` 一致 | `request_id` 被 `..` 丢弃（`http_proxy.rs:418/431/444`），无断言无日志 | 不一致时错误可观测 + 该流被 reset |
| **R7** | 计数一律 RAII 释放，且票据绑到**响应 body** 生命周期 | ✅ 已做对（`metrics.rs:203`, `registry.rs:196`, `http.rs:312`）——**照搬** | e2e：客户端中断 + **上游静默**场景下，槽位须在秒级归零（现有实现会拖到 120 s，见 §4.7） |
| **R8** | 原子占位用 CAS，无 check-then-act | ✅ 已做对（`registry.rs:170`, `metrics.rs:57`）——**照搬**；HTTP 侧有良性误拒窗口 | 并发 N 请求恰好 limit 通过、零误拒 |
| **R9** | 背压端到端有界，且**按字节**而非按条数 | 通道 `mpsc(32)` 条数有界、字节无界（`http_proxy.rs:221`） | 慢客户端压测下进程 RSS 有上界 |
| **R10** | 超时矩阵完整，区分"空闲"与"总时长"；取消**即时**传播 | 无总时长上限；无 TLS 握手/`open_bi`/写帧/控制流超时；取消可延迟 120 s（`http_proxy.rs:419`） | 静默上游的取消在秒级释放槽位，而非等 `idle_timeout` |
| **R11** | 可归因性：日志/指标带 `request_id` + **`agent_id`** + `model`；429 分源；延迟有分位数 | 无 `agent_id`（结构体无此字段）；429 三源合一；只有求和值；`hlmg_agents` 语义错误 | 从 `/metrics` 能区分三种 429；从日志能回答"路由到哪个 edge" |
| **R12** | 生命周期与运维：drain 式关闭；healthz 豁免闸门；`agent_id` 全局唯一 | SIGTERM 只 abort 2 个监听任务（`lib.rs:157-161`）；healthz 过闸门；默认 `agent_id` 会撞车 | SIGTERM 下在途流收到明确错误事件而非硬切；两进程读同一份配置能共存 |

---

## 7. 分阶段路线

```
M0  不可逆决策清单 + 帧协议文档（含 golden bytes）+ 传输参数表
M1  最小链路：QUIC + mTLS + 非流式转发
M2  流式：SSE 逐块透传 + Cancel 传播
M2.5 并发体检：传输默认值显式化 + 高并发压测        ← 本项目最贵的教训，前移到此
M3  加固：HTTPS + 令牌桶 + 分层 admission
M4  观测：指标（带标签）+ request_id/agent_id 贯通 + 日志落盘 + 深度 healthz
M5  模型路由：按 model 过滤候选、精确优先、`*` 兜底、同组最少负载
```

### 各阶段门禁

| 阶段 | 门禁（不过不进下一阶段） |
|---|---|
| M0 | 三条不变量 + 12 条硬性要求成文；帧格式有 golden bytes；传输参数表每一项都有"为什么" |
| M1 | 单条请求跑通；错误码语义明确；无泄漏（守卫 RAII 齐全） |
| M2 | SSE `[DONE]` 完整；客户端中断 → 上游取消（**含上游静默场景**）；R3/R4 测试全绿 |
| **M2.5** | **每个 transport 参数显式声明并压测验证**；R9（背压有界）在慢客户端压测下 RSS 有上界；R8 并发不超卖 |
| M3 | 三种 429 可区分；`/healthz` 豁免闸门 |
| M4 | 从日志能回答"路由到哪个 edge"；延迟有分位数；R11 全绿 |
| M5 | 双 edge 异构模型 e2e 覆盖（路由正确性、聚合列表、404、400） |

### M5 的关键：**数据先行**

注册条目的数据结构**从 M1 就存 `models`**。这样 M5 是"读已有字段"而不是"改注册协议"。
现有项目这一点属于**碰巧做对了**；重建时要**刻意**——任何新增能力都应该是读已有字段。

---

## 8. 验证体系

四层分工，各管一段：

| 层 | 工具 | 覆盖 |
|---|---|---|
| 单测 | `cargo test` | 帧 golden bytes + 四格 EOF 边界 + **取消安全（DribbleReader 范式）** + 准入 CAS 并发 |
| e2e | 单进程起全栈 | 真实 QUIC+mTLS：全链路、SSE `[DONE]`、双向 Cancel（含上游静默）、admission、admin CRUD、OpenAI 错误语义、metrics、**坏帧后 agent 仍在册** |
| 微基准 | Criterion | 帧编解码（**必须覆盖实际生产读路径**，含分配计数）、密码学原语 |
| 宏观压测 | k6 | SSE 长流并发、QPS、admission 闸门、**慢客户端 + 快上游**的内存上界 |

**两个必须照搬的细节**：

1. **时序敏感的 e2e 用串行执行**（现有项目用 `#[serial]`）；
2. **压测断言必须区分 429 和 5xx**。429 = 准入按设计拒绝（可接受），
   5xx = 真故障。现有项目的 k6 脚本专门做了状态码分类计数
   （`scripts/bench-k6/*.js`），否则压测报告无法判读。

**一个必须避免的覆盖盲区**：现有项目的 `FrameReader::next`（修取消安全时新写的**生产主路径**）
**完全没有基准**，而 `TODO.md` 的 postcard→protobuf 迁移决策门槛正依赖那份 bench。
基准要覆盖真实热点，并且把编码与解码**分开测量**（现有 bench 把两者混在一次 `iter` 里）。

---

## 9. 重建差异清单：第一天就要做、原项目后补的事

| 项 | 原项目实际 | 重建 |
|---|---|---|
| 配置格式 | CLI flags → YAML（`10f90de`） | 第一天 YAML |
| 持久化 | JSON → SQLite（`3b0665e`），中间误提交 `keys.db`（`653decd`） | 第一天 SQLite + 立刻 gitignore |
| `request_id` 贯通 | 后期才补（`b2e3a5d`），要改所有路径 | M1 就贯穿 HTTP/帧/日志 |
| `agent_id` 进日志/指标 | 后期才补（`745e8e8` 时结构体还没有该字段） | M1 就带上（同时解决 R11 和 R12 的一半） |
| 日志落盘 | 后期才配 systemd 文件（`e558f53`） | 部署文档 M4 就带 |
| 传输默认值 | 压测撞墙才发现（`114ac8f`，quinn 时代；换栈后又以"排队"形式重现一次） | M2.5 提前显式化——**窗口参数也要设，不只是流额度** |
| 帧的 version 校验 | 至今只写不读 | M0 就定，M1 就校验 |
| 单流错误隔离 | 至今毒化整条连接 | M1 就做对 |

---

## 10. 附录：事故史 → 设计约束映射

想知道某条要求为什么存在，查这里。全部来自现有项目的真实 commit 与实测。

| # | 事故 / 缺陷 | 症状有多隐蔽 | 对应约束 |
|---|---|---|---|
| 1 | `read_exact` 在 `select!` 中落败丢半读字节 → 长度前缀错位 → `Cancel` 被静默丢弃（`d3f5360`） | 两端进程健康、日志正常，只有 token 账单和延迟在变 | §3.3, §6-R3 |
| 2 | 客户端中断 → 请求 future 被 hyper drop → 槽位永久泄漏 → 此后全部 429 直到重启（`f7d4032`） | `/healthz` 也 429，看起来像"网关死了" | §4.2, §6-R7 |
| 3 | 两台 edge 用同一 `agent_id` → 网关"同名接管"关旧连接 → agent 把被踢当干净断开、退避重置 → **每 ~500 ms 互踢、永不收敛** → 长于踢连接周期的请求全部 502 | 两侧进程健康、管理页恒显示"1 个 agent 在线"、日志只有反复的 reconnect。**唯一信号是 `hlmg_agent_connections_total` 在飞涨——而它是 counter，不看 rate 注意不到** | §6-R12, §5.1 |
| 4 | 并发请求各自读到对方自增后的计数 → 双双被误拒（`b000b3b`） | 串行单测永远发现不了 | §4.3, §6-R8 |
| 5 | quinn 时代 `max_concurrent_bidi_streams` 默认 100 → 高并发延迟爆炸、连接被重置（`114ac8f`）。**换 `s2n-quic` 后同一根源又以"额度排满只是排队"重现**（§4.5 注 ①） | 第一次只在压测暴露；第二次更隐蔽：进程、日志、systemd 全正常，只有 503 与重连数在涨 | §4.4, §4.5, §6-R7 |
| 6 | 每请求 inline 跑 argon2（10–30 ms / 19 MiB）→ 占住 async worker → 连正在流式的 SSE 一起拖慢（`8050af3`） | 表现为"莫名卡顿"，非错误 | §7 热路径；重建时再加**认证缓存** |
| 7 | 同一份请求体被全量 JSON 解析 3 次 | 无错误，纯成本 | §4.8 附注（单位成本） |
| 8 | QUIC 隧道入口静默死亡：进程、HTTP、systemd 全显示健康、日志一行都没有 | **无任何信号**，只能靠专门造的 gauge | §5.1（Drop 守卫型 gauge） |
| 9 | 误提交 `keys.db`（`653decd`） | — | §9（第一天 gitignore） |
| 10 | 文档漂移：`DESIGN.md` 写的帧格式 `[frame_type: u8][request_id: u64][payload]` 与实现（长度前缀 + postcard 枚举）不符 | 新人按文档理解协议会全错 | §3.1（golden bytes 而非手写文档）；**该漂移已修正**，但"用契约测试而不是手写文档来固化线格式"这条要求不变 |
| 11 | 测试注释声称 `read_frame` 与 `FrameReader`"语义完全相同"，实测不成立且无测试覆盖 | 差异会被误判为"上游正常收流" | §3.4, §6-R4 |
| 12 | 同项目两个同名 `NEXT_REQUEST_ID` 静态计数器 → 兜底路径可撞号，破坏"三方对账一致"的承诺 | 只损害日志可读性 | §5.2, §6-R11 |

---

## 附：工作区规划

```
<project>/
├── Cargo.toml              # workspace
├── DESIGN.md               # 系统是什么
├── REBUILD.md              # 本文：重建规格
├── crates/
│   ├── proto/              # 帧类型 + 编解码 + 分帧（含 golden bytes 测试）
│   ├── gateway/            # 公网网关二进制（HTTP 入口 + QUIC server + 治理）
│   ├── agent/              # 边缘 agent 二进制（QUIC client + 上游转发）
│   └── mock-llm/           # 假 OpenAI 兼容上游（无真实模型也能打通链路）
├── web/                    # 管理面板（可选，独立工程）
├── certs/                  # CA 与签发**脚本**（不进仓库私钥）
└── deploy/                 # systemd 单元 / 容器编排
```

**开发环境必须能一键起全栈**（假上游 + 网关 + agent，日志落文件），
否则每条 e2e 都要手工搭环境，测试就写不下去。这是现有项目 `Makefile` 做对的一件事。
