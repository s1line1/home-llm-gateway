target
.cargo-home
# 前端构建产物（web/dist）与发布打包产物（dist/）都不进上下文：Dashboard 现在由 Dockerfile 的
# `web-builder` 阶段**在镜像里现场构建**（源在 `web/` 里），宿主机上那份既有产物只会白白增大
# 上下文、并让 Rust 的源码层缓存失效。
# **必须带 `**/`**：不带斜杠的 `dist` 只匹配**上下文根目录**下的 dist，`web/dist` 匹配不到
# （dockerignore(5)：`temp?` 这类模式的作用域是 root 目录；`**` 可匹配零个目录，
# 所以 `**/dist` 同时覆盖根的 dist/ 与 web/dist）。
**/dist
.git
# 本地开发/基准的临时目录（本检出 ~98MB：日志、k6 CSV、pid、证书实验）：进上下文纯属浪费，
# 而且每次 make dev / 基准都会动它 ⇒ 让下面的 `COPY . .`（源码层）缓存整体失效。
.tmp
# macOS 元数据（本检出根目录就有一个 .DS_Store）
.DS_Store
**/.DS_Store
# 前端依赖：装完后 web/node_modules 有 100MB+，占仓库体积九成。镜像不用它，
# 但每次构建都会把它当上下文传给 builder，还容易让清单层/源码层的缓存失效。
**/node_modules
keys.db
keys.db-wal
keys.db-shm
gateway-config.yml
agent-config.yml
**/config.yml
**/gateway-config.yml
**/agent-config.yml
# 开发证书（**含私钥**）：`certs/out/` 是 `certs/gen-dev.sh` 生成的，`.gitignore` 挡住了它们，
# 但 **Docker 不读 `.gitignore`** —— 而构建上下文是仓库根，`COPY . .`（源码层）会把整个上下文
# 拷进 builder。运行阶段只 COPY 二进制与 dist，所以**最终镜像里没有**它们；但 builder 层、
# daemon 的构建缓存、`--target builder` 与上下文 tar 里都会留着私钥（重扫 H1-1）。
# 只排 `out/`：`certs/gen-dev.sh` 是源码，留着无妨（也免得以后有人以为证书目录整个不能用）。
certs/out
