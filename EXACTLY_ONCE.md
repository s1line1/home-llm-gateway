# 精确一次重试：协议级去重设计（提案）

> 状态：**提案，未实现**。本文只描述设计、取舍与落地步骤，代码改动留待确认后再做。
> 相关实现：`crates/gateway/src/proxy/mod.rs`（重试循环）、`crates/agent/src/stream.rs`（帧处理）、
> `crates/proto/src/io.rs`（帧读写）。

## 1. 为什么要做

网关现在对**建立阶段**的失败已经会换 agent 重试（开流失败、写请求帧失败）——
那两条路径上请求帧从未完整送达 agent，重放不可能产生重复副作用。

但**响应头超时（`504 upstream timed out`）不重试**，因为请求可能已经在模型侧执行：
重试一次就可能出现两次计费、两次生成，而 `temperature > 0` 时两次结果还不一样。
实测里 768 并发下这类失败占少数（失败几乎全是建立阶段的 502），所以现状可以接受；
但它意味着"agent 收到了请求、然后就卡住了"这种最典型的故障，客户端拿到的是错误而不是自愈。

**本文要解决的问题就是：如何让"响应头超时"也能安全重试。**

## 2. 目标与非目标

**目标**

- 响应头超时也能重试一次，且**不产生第二次模型调用**。
- 客户端语义不变（OpenAI 兼容），不需要客户端配合。

**非目标（写清楚，避免误期待）**

- **不做跨实例、跨重启的 exactly-once**。去重表在 agent 进程内存里，agent 重启即失效，
  退化为今天的行为（at-least-once）。
- **不做流式响应的结果重放**（原因见 §4.4）。
- **不能回收第一次调用的算力成本**。去重只能避免"第二次调用"，第一次已经花掉的
  GPU 时间无法找回——这是物理限制，不是设计缺陷。
- 不改动上游模型服务的调用方式（不假设 vLLM/Ollama 支持幂等键）。

## 3. 设计所依赖的既有事实

| 事实 | 位置 | 对设计的意义 |
|---|---|---|
| 帧格式 `[u32 长度][postcard 载荷]`；agent 先读满整帧再动上游 | `proto/src/io.rs`、`agent/src/stream.rs` | **建立阶段失败必然"未送达"**——这是现有重试安全性的根据，也是本设计不必覆盖的区间 |
| agent 每条流一个任务，读到 `ProxyRequest` 后才构造上游 HTTP 请求 | `agent/src/stream.rs` | 去重表的插入点就在"读到完整帧之后、调用上游之前" |
| `Cancel` 帧通过 watch 通道触发上游取消 | `agent/src/stream.rs` | 去重命中"等待复用"时要与取消联动（见 §4.5） |
| 请求体在网关侧已整包在手（`Bytes`） | `gateway/src/proxy/mod.rs` | 重放不需要重新读客户端 |
| `request_id` 现在是 `x-request-id` 的数字部分，否则取网关进程内自增号（唯一分配器） | `gateway/src/request_id.rs`（`http/observability.rs`、`proxy/mod.rs` 共用） | **只在单实例内唯一**，多实例/重启会撞号 → 必须换标识（§4.1） |

## 4. 设计

### 4.1 请求标识：必须全局唯一且在重试间不变

新增 `request_uid`，与现有 `request_id` 并存：

- `request_id`（u64）：**日志对账**用，保持现状（HTTP 日志 / 隧道帧 / 响应头三方一致）。
- `request_uid`：**去重**用，要求
  1. 全局唯一（跨网关实例、跨重启）；
  2. **同一次用户请求换 agent 重试时保持不变**——这是整套机制的核心前提。

推荐 `UUIDv7`（128 位，含时间戳，无需中心分配、无需协调），或 `(gateway_instance_id, counter)`
（更省字节，但要保证 instance_id 持久且不重复）。帧里加一个字段即可：

```rust
ProxyRequest { request_uid: [u8; 16], request_id: u64, method, path, headers, body }
```

> ⚠️ 这是帧协议的**不兼容变更**：postcard 结构体加字段会让新旧版本互不可读。
> 落地时要么走 `proto` 的版本协商（见 `DESIGN.md` §4.2 的帧协议演进），
> 要么把网关与 agent 的升级编排成"先升 agent、再升网关"（agent 侧对新字段宽容）。

### 4.2 agent 侧去重表

```rust
enum DedupeState {
    /// 已经在上游执行中：第二个同 uid 请求不重复调用上游。
    InFlight { started: Instant, done: Arc<Notify> },
    /// 已完成：仅当"没有产生任何响应字节"时才可能被复用（见 §4.4）。
    Done { status: u16, since: Instant },
}

struct DedupeTable {
    map: Mutex<HashMap<[u8; 16], DedupeState>>,
    max_entries: usize,   // 容量上限
    ttl: Duration,        // 重试窗口的量级：秒级即可（网关重试在几百 ms 内发生）
}
```

处理流程（`handle_stream` 读到完整 `ProxyRequest` 之后）：

1. `InFlight` 命中 → 走 §4.3 的策略（等待或拒绝），**绝不重复调用上游**；
2. `Done` 命中且未过期 → 仅"零字节响应"可复用（§4.4）；
3. 都未命中 → 插入 `InFlight`，正常处理；**在拿到响应头的那一刻**把状态改成
   "已产出"（此时起不再允许任何复用）。

淘汰：`TTL` + `max_entries` 双重约束。表满时**新请求不记录**（退化为 at-least-once）
并打指标——绝不因为表满而拒绝正常请求。

### 4.3 重复请求怎么处理（需要产品决定）

| 方案 | 行为 | 优点 | 缺点 |
|---|---|---|---|
| **A 等待复用** | 第二个请求挂在 `InFlight` 的 `Notify` 上，等第一个完成，然后共享同一次上游调用的结果 | 最接近 exactly-once；客户端拿到的是"慢一点的成功" | 延迟转嫁给客户端；第一个失败时第二个也要拿到同样的失败；需要超时兜底 |
| **B 明确拒绝** | 第二个请求返回 409 `duplicate request in flight`，网关据此**不再重试**（或换下一个 agent 前先判定） | 实现最简、行为可预测、不引入新的等待语义 | 客户端可能仍拿到错误（取决于网关怎么处理 409） |
| **C 结果回放** | `Done` 且零字节响应 → 把缓存的状态码/头/体重放 | 覆盖"上游很快返回、重试发生在其后"的情形 | 只适用于非流式；要管内存 |

**建议：A + C**。理由：A 覆盖"重试发生在第一个尚未返回时"（最常见），C 覆盖"第一个已经返回、
重试才到"的窄窗口，两者合起来能把重复调用压到接近零；B 只作为 §4.4 之后的兜底
（已经产出响应字节时的重复）。

### 4.4 与流式响应的冲突（本设计的核心难点）

SSE 一旦开始输出：

- 结果**无法重放**给第二个请求——除非把整条响应缓存下来（内存 O(响应长度)），
  而且此时第一个客户端已经收到了前半段，"重放"对第一个客户端毫无意义；
- 因此去重只能在**"上游尚未产出任何响应字节"**的窗口内生效。

**结论（第一期）**：只对"还没拿到响应头"的请求做去重复用/拒绝；一旦产出响应头，
重复请求一律按 B（明确拒绝）。想要更强的一致性需要把 agent 变成"响应广播器"
（第二个请求订阅同一上游流），复杂度和内存代价都上一个台阶，列在 §7 阶段 4 评估。

### 4.5 与取消（Cancel）的交互

- 去重表里的 `Cancel` 语义要明确：**取消应该取消"那一次上游调用"，而不是某一个订阅者**。
  如果采用 A（等待复用），第二个请求被客户端取消时**不得**中断第一个请求的上游调用；
  只有"所有订阅者都取消"（或第一个请求的发起方取消）才真正发 `Cancel`。
- 实现上：`InFlight` 记录 `subscribers: usize`，归零才取消上游。

### 4.6 网关侧的配合

| 失败点 | 现状 | 有去重之后 |
|---|---|---|
| 开流失败 / 写帧失败 | 重试 | 重试（不变） |
| **响应头超时** | **不重试** | **重试**（去重表保证不重复调模型） |
| 客户端取消 | 不重试 | 不重试 |
| 响应体中途断流/超时 | 不重试 | **仍不重试**（已产出字节，无法重放） |

其它配合项：

- 重试次数上限（建议 1 次，即总尝试 2 次）、是否需要退避（几百 ms 即可，避免同时打到同一 agent）；
- 收到 `409 duplicate in flight` 时**不重试**（重复就是重复，再换也没用）；
- 触发重试前先 `evict` 掉失败的那条连接（现状已有），并用 `try_acquire_excluding` 排除它。

## 5. 可观测性（与去重一起上，否则无法验证）

**agent 侧**

- `hlmg_agent_dedupe_hits_total{kind="in_flight"|"done"}`：命中去重的次数（= 避免了重复上游调用的次数）；
- `hlmg_agent_dedupe_rejects_total`：因"已产出响应字节"而拒绝重复的次数；
- `hlmg_agent_dedupe_entries`：表内条目数（gauge），逼近上限要告警。

**网关侧**

- 复用现有 `hlmg_tunnel_retries_total{outcome}`；新增触发原因维度（`reason="setup"|"head_timeout"`）；
- `hlmg_retry_duplicate_rejected_total`：因 409 而放弃重试的次数。

**验证口径**：改造前后的对比必须以"上游被调用次数"为准，而不是只看客户端成功率——
`hlmg_agent_dedupe_hits_total` > 0 同时"模型侧调用计数"不增长，才证明去重真的生效。

## 6. 内存与容量

| 项 | 量级 | 约束 |
|---|---|---|
| `InFlight` 条目 | ≈ 100 字节（uid + 时间戳 + Notify） | 与在途请求数同阶，无需额外限制 |
| `Done` 缓存（仅非流式） | 响应体大小 | 单条上限（如 1 MiB）+ 总量上限（如 32 MiB），超限不缓存（退化为 at-least-once） |
| 流式响应 | **不缓存** | 只保留"已产出"标记 |

TTL 建议 30–60s：只需覆盖"网关重试窗口"（几百 ms）加上时钟抖动余量；
过长会让内存被无用的历史条目占据。

## 7. 分阶段落地

| 阶段 | 内容 | 依赖 | 风险 |
|---|---|---|---|
| **1（已完成）** | 建立阶段失败换 agent 重试 | — | 低（已上线） |
| **2** | `request_uid` + agent 去重表（InFlight 等待/拒绝）；网关在响应头超时上启用重试 | 帧协议变更 + 两端同步升级 | 中：协议不兼容，需要升级编排 |
| **3** | 非流式结果缓存回放（方案 C） | 阶段 2 | 低 |
| **4（评估）** | 流式"订阅转发"（第二个请求订阅同一上游流） | 阶段 2 | 高：内存与取消语义复杂，先评估收益 |

## 8. 测试计划

1. **协议单测**：同一 uid 连续两次请求 → 断言上游被调用**恰好一次**（用 mock 计数）。
2. **e2e（本次的核心场景）**：假 agent 收到请求后不回响应头（模拟 504）→ 网关重试到第二个 agent
   → 断言客户端最终成功，且**模型侧总调用次数为 1**（第一个 agent 的调用被去重表挡掉）。
3. **负例**：`max_entries` 打满 / TTL 过期 → 允许重复（指标可观测），不得 panic、不得拒绝正常请求。
4. **取消语义**：两个订阅者中一个取消 → 上游调用**不**被中断；全部取消 → 才发 `Cancel`。
5. **降级路径**：处理中重启 agent → 退化为 at-least-once，无崩溃、无死锁。
6. **协议兼容**：新旧版本混跑时的行为必须明确（拒绝连接 / 降级）并写进部署说明。

## 9. 本方案不解决的问题

- **不能保证跨重启、跨实例的 exactly-once**（去重表是进程内存）。
- **不能回收第一次调用的算力**（只避免第二次）。
- **不能保证重复请求得到相同文本**（`temperature > 0` 时上游本就非确定性；去重保证的是
  "不重复调用"，而不是"结果一致"）。
- **不能覆盖"已产出响应字节"之后的失败**（无法重放，只能报错）。

## 10. 备选方案与否决理由

| 备选 | 否决理由 |
|---|---|
| 让 504 也直接重试，不做去重，靠概率接受重复 | 重复计费对用户不可接受；且 `temperature>0` 时两次结果不同，语义上无法解释 |
| 在网关侧对同一 uid 排队，保证不并发打到两个 agent | 治标不治本：agent 卡死/半死时网关仍无法判断该不该换人，最终还是要重试 |
| 依赖上游（vLLM/Ollama）的幂等键 | 上游没有这个语义，且会把复杂度转嫁给模型服务 |
| 把重试窗口做成"只要没收到响应头就无限重试" | 会在上游真的慢（长思考）时放大负载，必须限次数 |
